<%= link_to "<<<ARTIST", edit_artist_path(@artist) %>
<h1> <%= @artist.name %> </h1>

<% @songs.each do |song| %>
    <li><%= song.name %> <%= link_to 'edit', edit_artist_song_path(@artist, song)%> <%= link_to 'delete', artist_song_path(@artist, song), method: :delete %></li>
  <% end %>
<%= link_to "<<<ARTIST", edit_artist_path(@artist) %>
<h1><%= @song.name %></h1>
<%= form_with(model: [@artist, @song], local: true) do |f| %>
 <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :genre %>
  <%= f.text_field :genre %>

  <%= f.submit %>
<% end %>